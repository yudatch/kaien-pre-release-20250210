# マルチサイトスクレイパー 設定ガイド

## 概要
このアプリケーションは、複数のECサイトから商品情報をスクレイピングし、ロゴ検出機能を使って商品画像をフィルタリングするツールです。

## 動作環境
- Docker と Docker Compose がインストールされていること
- インターネット接続が可能であること

## 初回起動手順

### 1. リポジトリのクローン
```bash
git clone <リポジトリURL>
cd logo-scraper-app
```

### 2. Dockerコンテナの起動
```bash
docker-compose up -d
```

### 3. アプリケーションへのアクセス
ブラウザで以下のURLにアクセスします：
```
http://localhost:8501
```

## サイト設定の追加方法

### 1. UIからの追加
1. アプリケーションの「サイト設定」ページに移動します
2. 「新規サイト追加」フォームに必要情報を入力します：
   - サイト名：サイトの識別名
   - URL：サイトのベースURL
   - ロゴ判定閾値：ロゴ検出の感度（0.0〜1.0）
   - スクレイピングセレクタ：各要素のCSSセレクタ

### 2. サンプル設定ファイルの利用
サンプル設定ファイルを参考に、新しいサイトの設定を作成できます。
サンプルファイルは `config/samples/` ディレクトリにあります。

#### セレクタの設定方法
スクレイピングセレクタは以下の形式で指定します：
```json
{
  "search_box": "#searchInput",
  "search_button": "#searchButton",
  "product_list": ".items",
  "name": ".title",
  "price": ".price",
  "image": "img.product_image @src"
}
```

各セレクタの説明：
- `search_box`: 検索ボックスのセレクタ
- `search_button`: 検索ボタンのセレクタ
- `product_list`: 商品リストのセレクタ
- `name`: 商品名のセレクタ
- `price`: 価格のセレクタ
- `image`: 画像のセレクタ（属性を指定する場合は `@属性名` を追加）

## 検索方法

1. 「検索」ページに移動します
2. プルダウンメニューからスクレイピング対象のサイトを選択します
3. 検索キーワードを入力します（複数キーワードはカンマ区切りで指定可能）
4. 「検索」ボタンをクリックします
5. 処理状況が表示され、完了すると結果が表示されます

## 結果の確認とエクスポート

1. 検索完了後、「結果を表示」ボタンをクリックします
2. 「結果管理」ページで検索結果を確認できます
3. 「CSVエクスポート」ボタンをクリックすると、結果をCSVファイルとしてダウンロードできます
4. 「ロゴ検出結果のみ表示」チェックボックスをオンにすると、ロゴが検出された商品のみを表示できます

## トラブルシューティング

### アプリケーションが起動しない場合
```bash
# ログの確認
docker-compose logs

# コンテナの再起動
docker-compose down
docker-compose up -d
```

### スクレイピングがうまくいかない場合
1. サイト設定のセレクタが正しいか確認してください
2. サイトの構造が変更された可能性があります。開発者ツールでセレクタを確認し、更新してください
3. 一部のサイトはスクレイピングを制限している場合があります

### ロゴ検出の精度が低い場合
ロゴ判定閾値を調整してください。値を下げるとより多くのロゴが検出されますが、誤検出も増加します。

## 高度な設定

### 設定ファイルの編集
`config/config.json` ファイルを編集することで、アプリケーションの動作を調整できます：

```json
{
  "app_name": "マルチサイトスクレイパー",
  "version": "1.0.0",
  "max_search_history": 10,
  "default_logo_threshold": 0.5,
  "retry_count": 3,
  "auto_delay": true
}
```

- `max_search_history`: 保存する検索履歴の最大数
- `default_logo_threshold`: デフォルトのロゴ判定閾値
- `retry_count`: スクレイピング失敗時の最大リトライ回数
- `auto_delay`: サイト応答速度に応じた自動待機時間の有効/無効
